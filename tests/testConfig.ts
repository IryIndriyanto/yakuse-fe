// Command for running the tests with playwright:
// 1. npx playwright test for running the tests
// 2. npx playwright show-report for viewing the test report
// 3. npx playwright test --ui for running the tests in headful mode


// This file is used to store the configuration for the tests. It is used to store the base URL for the API calls.

// export const baseURL = 'http://localhost:3000';

export const baseURL = 'https://yakuse.vercel.app';
